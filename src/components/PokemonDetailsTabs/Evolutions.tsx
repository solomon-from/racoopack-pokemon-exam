import React from 'react';
import { Text } from '@tamagui/core';
import { View } from 'react-native';

const Evolutions = ({ details }) => {
  return (
    <View>
      {/* Placeholder for evolutions. You can replace this with actual evolution details if available. */}
      <Text fontSize={18} color="$text">
        Evolutions not available in the provided data.
      </Text>
    </View>
  );
};

export default Evolutions;

